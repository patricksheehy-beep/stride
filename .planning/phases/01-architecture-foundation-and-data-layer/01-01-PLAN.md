---
phase: 01-architecture-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - vite.config.js
  - package.json
  - src/app.js
  - src/core/event-bus.js
  - src/core/state.js
  - src/core/config.js
  - src/core/cache.js
  - src/map/map-manager.js
  - src/map/layers.js
  - styles/main.css
  - vitest.config.js
  - tests/setup.js
  - tests/core/event-bus.test.js
  - tests/core/state.test.js
  - tests/core/cache.test.js
autonomous: true
requirements:
  - ARCH-01
  - ARCH-04

must_haves:
  truths:
    - "App loads from a Vite dev server with modular ES imports (not a single index.html blob)"
    - "Modules communicate via EventBus pub/sub without direct cross-imports"
    - "State changes through Proxy store trigger automatic event notifications"
    - "IndexedDB cache stores and retrieves data with TTL-based expiration"
    - "Leaflet map renders with OSM tiles on page load"
  artifacts:
    - path: "src/core/event-bus.js"
      provides: "Central pub/sub EventBus"
      exports: ["eventBus"]
      min_lines: 15
    - path: "src/core/state.js"
      provides: "Reactive Proxy-based state store"
      exports: ["store", "subscribe"]
      min_lines: 30
    - path: "src/core/config.js"
      provides: "API key management and app constants"
      exports: ["getApiKey", "setApiKey", "config"]
      min_lines: 15
    - path: "src/core/cache.js"
      provides: "IndexedDB wrapper with TTL caching"
      exports: ["getCached", "setCache", "clearCache"]
      min_lines: 30
    - path: "src/map/map-manager.js"
      provides: "Leaflet map initialization"
      exports: ["initMap"]
      min_lines: 15
    - path: "src/app.js"
      provides: "Entry point wiring all core modules"
      min_lines: 10
  key_links:
    - from: "src/core/state.js"
      to: "src/core/event-bus.js"
      via: "import { eventBus }"
      pattern: "eventBus\\.emit\\('state:"
    - from: "src/app.js"
      to: "src/core/event-bus.js"
      via: "import { eventBus }"
      pattern: "import.*event-bus"
    - from: "src/app.js"
      to: "src/map/map-manager.js"
      via: "import { initMap }"
      pattern: "import.*map-manager"
---

<objective>
Scaffold the Vite project, install all dependencies, and build the core infrastructure modules (EventBus, State Store, Config, Cache) plus a minimal Leaflet map. This creates the foundation every subsequent plan depends on.

Purpose: Establish the modular ES Module architecture (ARCH-01) and IndexedDB caching infrastructure (ARCH-04) that all data and routing modules will use.
Output: A running Vite dev server with core modules, Leaflet map, and passing unit tests.
</objective>

<execution_context>
@C:/Users/PatrickSheehy/Desktop/Stride Project/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PatrickSheehy/Desktop/Stride Project/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-architecture-foundation-and-data-layer/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Vite project and create core infrastructure modules</name>
  <files>
    package.json, vite.config.js, index.html,
    src/app.js, src/core/event-bus.js, src/core/state.js,
    src/core/config.js, src/core/cache.js,
    vitest.config.js, tests/setup.js,
    tests/core/event-bus.test.js, tests/core/state.test.js,
    tests/core/cache.test.js
  </files>
  <read_first>
    .planning/phases/01-architecture-foundation-and-data-layer/01-RESEARCH.md
    .planning/research/STACK.md
  </read_first>
  <behavior>
    EventBus:
    - eventBus.on('test', cb) registers a listener; eventBus.emit('test', {x:1}) calls cb with {x:1}
    - eventBus.on() returns an unsubscribe function; calling it removes the listener
    - eventBus.once('test', cb) fires cb exactly once then auto-removes

    State Store:
    - store.selectedMode = 'trail' emits 'state:changed' with {property:'selectedMode', value:'trail', oldValue}
    - store.selectedMode = 'trail' also emits 'state:selectedMode' with {value:'trail', oldValue}
    - Reading store.selectedMode returns 'trail'
    - subscribe(fn) calls fn(property, value) on every change; returns unsubscribe function

    Cache (IndexedDB):
    - setCache('trails', 'key1', {data:'test'}) stores data; getCached('trails', 'key1') returns {data:'test'}
    - getCached returns null for non-existent keys
    - getCached returns null for expired entries (TTL exceeded)
    - clearCache('trails') removes all entries from the trails store

    Config:
    - getApiKey('ors') returns null when no key is set
    - setApiKey('ors', 'test-key-123'); getApiKey('ors') returns 'test-key-123'
    - config.overpassEndpoint equals 'https://overpass-api.de/api/interpreter'
    - config.orsBaseUrl equals 'https://api.openrouteservice.org'
    - config.osrmBaseUrl equals 'https://router.project-osrm.org'
  </behavior>
  <action>
    1. Initialize the Vite project in the current directory (do NOT use `npm create vite` since we are in the project root already). Create package.json manually with:
       - name: "stride"
       - type: "module"
       - scripts: dev ("vite"), build ("vite build"), preview ("vite preview"), test ("vitest run"), test:watch ("vitest")
       - dependencies: leaflet@1.9.4, idb@8.0.3, @turf/helpers@7.3.4, @turf/distance@7.3.4, @turf/bbox@7.3.4, @mapbox/polyline@1.2.1, openrouteservice-js@0.4.1
       - devDependencies: vite@^8.0.0, vitest@^4.0.0, vite-plugin-pwa@^1.2.0, jsdom@^26.0.0

    2. Run `npm install` to install all deps.

    3. Create vite.config.js per STACK.md skeleton (base: '/stride/', VitePWA plugin with registerType: 'autoUpdate', map tile caching CacheFirst, API response StaleWhileRevalidate). Import defineConfig from 'vite' and VitePWA from 'vite-plugin-pwa'.

    4. Create index.html as the app shell:
       - DOCTYPE html, lang="en", meta charset UTF-8, meta viewport width=device-width initial-scale=1.0
       - title: "Stride - AI Running Route Generator"
       - link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
       - link rel="stylesheet" href="/styles/main.css"
       - div id="app" containing: div id="map"
       - script type="module" src="/src/app.js"

    5. Create styles/main.css with minimal styles:
       - * { margin: 0; padding: 0; box-sizing: border-box; }
       - html, body { height: 100%; font-family: system-ui, sans-serif; }
       - #app { height: 100%; display: flex; }
       - #map { flex: 1; min-height: 400px; }

    6. Create src/core/event-bus.js:
       - Class EventBus extends EventTarget
       - emit(type, detail={}) dispatches new CustomEvent(type, { detail })
       - on(type, callback) adds listener wrapping e => callback(e.detail), returns unsubscribe function that calls removeEventListener
       - once(type, callback) adds listener with { once: true }
       - Export singleton: export const eventBus = new EventBus();

    7. Create src/core/state.js:
       - Import { eventBus } from './event-bus.js'
       - Define initialState object: { userLocation: null, selectedMode: 'trail', requestedDistance: 5, trails: [], currentRoute: null, isGenerating: false }
       - Create Proxy-wrapped store:
         - set trap: save oldValue, set new value, if oldValue !== value emit 'state:changed' with {property, value, oldValue} AND emit `state:${property}` with {value, oldValue}, return true
         - get trap: return target[property]
       - Export subscribe(fn) that adds fn to a Set of listeners, called on each set with (property, value). Returns unsubscribe function.
       - Wire subscribe into the Proxy set trap: call each listener(property, value) after eventBus emissions
       - Export { store, subscribe }

    8. Create src/core/config.js:
       - Export function getApiKey(service): reads localStorage key `stride_api_${service}`, returns value or null
       - Export function setApiKey(service, key): writes localStorage key `stride_api_${service}`
       - Export const config = { overpassEndpoint: 'https://overpass-api.de/api/interpreter', overpassFallbackEndpoint: 'https://overpass.kumi.systems/api/interpreter', orsBaseUrl: 'https://api.openrouteservice.org', osrmBaseUrl: 'https://router.project-osrm.org', overpassTimeout: 300, cacheTTL: 24 * 60 * 60 * 1000, nominatimEndpoint: 'https://nominatim.openstreetmap.org' }

    9. Create src/core/cache.js using idb library:
       - import { openDB } from 'idb'
       - DB_NAME = 'stride-cache', DB_VERSION = 1
       - Store names: TRAIL_STORE = 'trails', ROUTE_STORE = 'routes'
       - DEFAULT_TTL = 24 * 60 * 60 * 1000 (24 hours)
       - getDB() returns lazy-initialized openDB promise with upgrade callback creating both object stores
       - Export getCached(storeName, key): gets entry, checks timestamp against TTL, returns data or null (deletes expired entries)
       - Export setCache(storeName, key, data, ttl = DEFAULT_TTL): puts { data, timestamp: Date.now(), ttl }
       - Export clearCache(storeName): clears all entries in the named store

    10. Create vitest.config.js: import { defineConfig } from 'vitest/config'; export default defineConfig({ test: { environment: 'jsdom', globals: true, setupFiles: ['./tests/setup.js'] } })

    11. Create tests/setup.js: import 'fake-indexeddb/auto' (install fake-indexeddb@^6.0.0 as devDependency for IndexedDB mocking). Add it to devDependencies and run npm install again.

    12. Write tests FIRST (RED phase) for all four core modules per the behavior spec above, then implement until GREEN:
        - tests/core/event-bus.test.js: test emit/on/once/unsubscribe
        - tests/core/state.test.js: test proxy set/get, event emission, subscribe/unsubscribe
        - tests/core/cache.test.js: test setCache/getCached with TTL, clearCache, null for missing keys

    13. Run `npx vitest run` to verify all tests pass.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists with "type": "module" and all listed dependencies
    - `npx vitest run` exits with code 0 and reports 0 failures
    - src/core/event-bus.js contains `export const eventBus`
    - src/core/state.js contains `export const store` and `export function subscribe`
    - src/core/config.js contains `export function getApiKey` and `export const config`
    - src/core/cache.js contains `export async function getCached` and `export async function setCache`
    - vite.config.js contains `VitePWA` and `base: '/stride/'`
    - index.html contains `type="module"` and `src="/src/app.js"`
  </acceptance_criteria>
  <done>All four core modules (EventBus, State, Config, Cache) exist with exported APIs, unit tests pass for all modules, Vite project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create Leaflet map module and wire app entry point</name>
  <files>
    src/map/map-manager.js, src/map/layers.js, src/app.js
  </files>
  <read_first>
    src/core/event-bus.js
    src/core/state.js
    src/core/config.js
    .planning/research/ARCHITECTURE.md
  </read_first>
  <action>
    1. Create src/map/layers.js:
       - Export const tileLayers = { osm: { url: 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', options: { attribution: '&amp;copy; OpenStreetMap contributors', maxZoom: 19 } } }

    2. Create src/map/map-manager.js:
       - Import L from 'leaflet' (Leaflet is loaded via CDN in index.html, available as window.L; for the ESM import to work with Vite, use: const L = window.L)
       - Import { eventBus } from '../core/event-bus.js'
       - Import { store } from '../core/state.js'
       - Import { tileLayers } from './layers.js'
       - let map = null;
       - Export function initMap(containerId = 'map'):
         - map = L.map(containerId).setView([37.7749, -122.4194], 13) (default San Francisco)
         - L.tileLayer(tileLayers.osm.url, tileLayers.osm.options).addTo(map)
         - Emit eventBus.emit('map:initialized', { map })
         - return map
       - Export function getMap() { return map; }

    3. Update src/app.js to be the wiring entry point:
       - Import { eventBus } from './core/event-bus.js'
       - Import { store } from './core/state.js'
       - Import { config } from './core/config.js'
       - Import { initMap } from './map/map-manager.js'
       - function init():
         - console.log('Stride initializing...')
         - const map = initMap('map')
         - console.log('Stride ready')
         - eventBus.emit('app:initialized')
       - Call init() when DOM is ready: document.addEventListener('DOMContentLoaded', init) or if (document.readyState !== 'loading') init(); else document.addEventListener('DOMContentLoaded', init);

    4. Verify `npx vite build` succeeds without errors. Note: Leaflet is loaded from CDN, not npm. In vite.config.js, do NOT try to bundle Leaflet. The map-manager.js should use `const L = window.L;` to access the global Leaflet object. Alternatively, install leaflet as npm dep and import it -- Vite will bundle it. Since leaflet is already in package.json dependencies, use `import L from 'leaflet'` and also add Leaflet CSS import at the top of app.js: `import 'leaflet/dist/leaflet.css'`. Remove the CDN link from index.html since Vite handles it.

    IMPORTANT: Since Leaflet is in package.json, use the npm import approach:
    - In src/app.js, add `import 'leaflet/dist/leaflet.css'` at the top
    - In src/map/map-manager.js, use `import L from 'leaflet'`
    - Remove the leaflet CDN link from index.html (Vite bundles the CSS)
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/map/map-manager.js contains `import L from 'leaflet'` and `export function initMap`
    - src/map/layers.js contains `export const tileLayers`
    - src/app.js contains imports from './core/event-bus.js', './core/state.js', './core/config.js', './map/map-manager.js'
    - src/app.js contains `import 'leaflet/dist/leaflet.css'`
    - index.html does NOT contain `unpkg.com/leaflet` CDN link
    - `npx vite build` exits with code 0
    - `npx vitest run` still passes (no regressions)
  </acceptance_criteria>
  <done>Leaflet map initializes in a modular ESM architecture, app entry point wires all core modules, Vite build succeeds, all existing tests still pass</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all unit tests pass (EventBus, State, Cache)
2. `npx vite build` -- builds successfully with no errors
3. Verify modular structure: `ls src/core/` shows event-bus.js, state.js, config.js, cache.js
4. Verify map module: `ls src/map/` shows map-manager.js, layers.js
</verification>

<success_criteria>
- Vite project scaffolded with all dependencies installed
- 4 core modules (EventBus, State, Config, Cache) created with documented exports
- Unit tests pass for EventBus, State, and Cache modules
- Leaflet map module created and wired through app.js entry point
- `npx vite build` succeeds without errors
- Clean ES Module architecture with no circular dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/01-architecture-foundation-and-data-layer/01-01-SUMMARY.md`
</output>
